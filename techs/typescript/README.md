# TypeScript

Use types and data ownership to prevent invalid states and accidental mutation.

Read these rules in full when they apply to your work. Validate changes with `code-rules library check` from the library root.
