name: Testing
description: Spend test effort where bugs are likely and costly, prove each behavior at the lowest layer that can, keep tests independent, and test observable behavior, regressions, and boundary cases.
whenToRead: When adding or changing behavior, fixing a bug, or writing and reviewing tests.
